# Parent-Budget Ledger: Coverage Matrices

Two linked inventories, taken from the code rather than from the physics. The
[budget contract](contract.md) fixes what they are measured against, and the
[plan](plan.md) says which pull request closes each row.

Nothing here is instrumentation. The point of the matrices is that no supported
configuration may claim closure while a row that mutates authoritative state is
still `unknown`.

## How to read a status

Two different things get called a status, and an earlier draft ran them
together. They are separated here, because a row can be mathematically zero and
still be uncollected, and reading one as the other is how an unmeasured term
becomes an assumed zero.

**Effect** — what the implemented equation does to the parent variable. This is
a proof obligation about the code, not a claim about the ledger. It is what the
`Δ` columns carry.

| Effect     | Meaning                                                                 |
|:---------- |:----------------------------------------------------------------------- |
| `measured` | A contribution that is not provably zero, so the ledger must measure it |
| `zero`     | Proven invariant zero, with the evidence named in the row               |
| `n/a`      | The path does not exist in a supported configuration                    |
| `unknown`  | Not yet established from the code; blocks the affected closure claim    |

**Collection** — whether the ledger actually reads the row. This is a fact
about the implementation, and in this pull request the answer is *none*: PR 1 is
design only and collects nothing. The `Collected by` column names the pull
request that changes it.

A `zero` in an effect column is therefore never evidence that anything has been
checked at runtime. Until its `Collected by` PR lands, the row is uncollected
whatever its effect says, and the closure claim it feeds stays blocked.

`Δ` columns are the effect on the **atmospheric** reservoir unless a row says
otherwise. Positive means addition.

## Matrix 1 — equation and exchange paths

The right-hand side has three channels, and confusing them is the first way to
lose a term.

  - `Yₜ` — the main explicit tendency, from `remaining_tendency!`.
  - `Yₜ_lim` — the *limited* explicit tendency, also from `remaining_tendency!`.
    `ClimaTimeSteppers` integrates it through the limiter, not alongside `Yₜ`.
  - `T_imp!` — `implicit_tendency!`, evaluated inside the Newton solve.

Plus `T_post_imp!`, a post-Newton correction, which is a channel of its own.

### Explicit, limited channel (`Yₜ_lim`)

| Path                                    | Fields                            | ΔM     | ΔW     | ΔE     | Collected by | Notes                                                                                                          |
|:--------------------------------------- |:--------------------------------- |:------ |:------ |:------ |:------------ |:-------------------------------------------------------------------------------------------------------------- |
| `horizontal_tracer_advection_tendency!` | `ρq_tot`, the categories, tracers | `zero` | `zero` | `zero` | PR 3         | Conservative horizontal divergence; global zero is an invariant of the operator and is **tested**, not assumed |
| `apply_tracer_hyperdiffusion_tendency!` | same                              | `zero` | `zero` | `zero` | PR 3         | Same test; DSS of the `∇²` cache happens inside `hyperdiffusion_tendency!`                                     |

Both rows are `unknown` for coverage purposes until PR 3 reads this channel at
all. This is the blocker the contract names: an adapter that reads only `Yₜ`
misses every row in this table.

### Explicit, main channel (`Yₜ`)

| Path                                                        | Fields                                 | ΔM                   | ΔW                   | ΔE                   | Collected by | Classification                                                                                                                                                                                                                                                                                                              |
|:----------------------------------------------------------- |:-------------------------------------- |:-------------------- |:-------------------- |:-------------------- |:------------ |:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `horizontal_dynamics_tendency!`                             | `ρ`, `ρe_tot`, `uₕ`                    | `zero`               | —                    | `zero`               | —            | conservative transport, tested                                                                                                                                                                                                                                                                                              |
| `explicit_vertical_advection_tendency!`                     | `ρ`, `ρe_tot`, tracers, `u₃`           | `zero`               | `zero`               | `zero`               | PR 3         | conservative transport, tested                                                                                                                                                                                                                                                                                              |
| `apply_hyperdiffusion_tendency!`                            | `ρe_tot`, `uₕ`, `ρtke`                 | —                    | —                    | `zero`               | —            | conservative, tested                                                                                                                                                                                                                                                                                                        |
| `viscous_sponge_tendency_*`                                 | `uₕ`, `u₃`, `ρe_tot`, tracers, `ρ`     | `measured`           | `measured`           | `measured`           | PR 3         | interior numerical source; the `ρ` leg is added only for the `ρq_tot` tracer                                                                                                                                                                                                                                                |
| `rayleigh_sponge_tendency_uₕ`                               | `uₕ`                                   | `zero`               | `zero`               | `zero`               | PR 3         | momentum only, `ρe_tot` prognostic and untouched — see the completeness note below                                                                                                                                                                                                                                          |
| `held_suarez_forcing_tendency_uₕ`                           | `uₕ`                                   | `zero`               | `zero`               | `zero`               | PR 3         | as above                                                                                                                                                                                                                                                                                                                    |
| `held_suarez_forcing_tendency_ρe_tot`                       | `ρe_tot`                               | `zero`               | `zero`               | `measured`           | PR 3         | idealized external heating                                                                                                                                                                                                                                                                                                  |
| `scm_coriolis_tendency_uₕ`                                  | `uₕ`                                   | `zero`               | `zero`               | `zero`               | PR 3         | momentum only                                                                                                                                                                                                                                                                                                               |
| `subsidence_tendency!`                                      | `ρe_tot`, `ρq_tot`                     | `zero`               | `measured`           | `measured`           | PR 3         | Prescribed large-scale input. Writes `ρe_tot`, `ρq_tot` and, under non-equilibrium, `ρq_lcl` and `ρq_icl`. It never writes `ρ`, so its mass effect is an **invariant zero** and the dry-air budget is open by construction                                                                                                  |
| `large_scale_advection_tendency_*`                          | `ρe_tot`, `ρq_tot`                     | `zero`               | `measured`           | `measured`           | PR 3         | `remaining_tendency!` adds only the `ρq_tot` and `ρe_tot` terms. No `ρ` term exists, so the mass effect is an invariant zero, as above                                                                                                                                                                                      |
| `external_forcing_tendency!`                                | `ρe_tot`, `ρq_tot`, `uₕ`               | `zero`               | `measured`           | `measured`           | PR 3         | Nudging and relaxation. `apply_Tq_forcing!` writes `ρe_tot` and `ρq_tot` only, so the mass effect is an invariant zero, as above                                                                                                                                                                                            |
| `vertical_diffusion_boundary_layer_tendency!`               | `ρe_tot`, tracers                      | `measured`           | `measured`           | `measured`           | PR 3         | only when `diff_mode == Explicit()`. An **interior** operator: `ᶜdiffdivᵥ` sets zero flux at the top and bottom faces, so it carries no boundary condition. `surface_flux_tendency!` carries the surface flux. Measured because the discrete interior integral is not exactly zero, not because anything crosses a boundary |
| `surface_flux_tendency!`                                    | `ρe_tot`, `ρq_tot`, `ρ`, `uₕ`          | `measured`           | `measured`           | `measured`           | PR 4         | **boundary crossing**; `Yₜ.c.ρ -= btt` is the mass leg                                                                                                                                                                                                                                                                      |
| `radiation_tendency!`                                       | `ρe_tot`                               | `zero`               | `zero`               | `measured`           | PR 4         | boundary crossing at TOA and surface                                                                                                                                                                                                                                                                                        |
| `microphysics_tendency!`, 0-moment                          | `ρq_tot`, `ρ`, `ρe_tot`                | `measured`           | `measured`           | `measured`           | PR 4         | Removal straight out of the column. `Yₜ.c.ρq_tot` and `Yₜ.c.ρ` take the same sink, so it is a **boundary crossing** for M and W both, with no receiving reservoir                                                                                                                                                           |
| `microphysics_tendency!`, 1-moment and non-equilibrium      | `ρq_lcl`, `ρq_icl`, `ρq_rai`, `ρq_sno` | `zero`               | `zero`               | `zero`               | PR 3         | Formation only redistributes the categories *inside* `ρq_tot`. It applies no source to `ρq_tot`, `ρ` or `ρe_tot`, so it is internal to all three. The fallout is a separate path, below                                                                                                                                     |
| `non_orographic_gravity_wave_apply_tendency!`               | `uₕ`                                   | `zero`               | `zero`               | `zero`               | PR 3         | momentum only                                                                                                                                                                                                                                                                                                               |
| `orographic_gravity_wave_apply_tendency!`                   | `uₕ`                                   | `zero`               | `zero`               | `zero`               | PR 3         | momentum only                                                                                                                                                                                                                                                                                                               |
| `surface_temp_tendency!`                                    | `Y.sfc.T`                              | `n/a`                | `n/a`                | `measured` (surface) | `n/a`        | the surface leg of the radiative and turbulent exchange                                                                                                                                                                                                                                                                     |
| `surface_precipitation_tendency!`                           | `Y.sfc.water`, `Y.sfc.T`               | `measured` (surface) | `measured` (surface) | `measured` (surface) | PR 4         | The surface leg of fallout, paired with `vertical_advection_of_water_tendency!`. Built from the cached surface rain and snow fluxes, a different quadrature from the atmospheric leg, so the pair is measured and any mismatch kept                                                                                         |
| `horizontal_/vertical_smagorinsky_lilly_tendency!`          | `ρ`, `ρe_tot`, tracers                 | `measured`           | `measured`           | `measured`           | PR 3         | diffusive; global zero only if the discrete operator has it, so measured                                                                                                                                                                                                                                                    |
| `horizontal_/vertical_amd_tendency!`                        | `ρ`, `ρe_tot`, tracers                 | `measured`           | `measured`           | `measured`           | PR 3         | as above                                                                                                                                                                                                                                                                                                                    |
| `horizontal_constant_diffusion_tendency!`                   | tracers                                | `measured`           | `measured`           | `measured`           | PR 3         | as above                                                                                                                                                                                                                                                                                                                    |
| `tracer_nonnegativity_vapor_tendency!`                      | `ρq_lcl`, `ρq_icl`, `ρq_rai`, `ρq_sno` | `zero`               | `zero`               | `zero`               | PR 3         | Category-only. It increments the four condensate fields and never writes `ρ`, `ρq_tot` or `ρe_tot`, so it is invariant zero for all three parent budgets. The vapour it draws on is implicit in `q_tot` minus the categories, not a field it writes                                                                         |
| `zero_velocity_tendency!`                                   | `uₕ`, `u₃`                             | `zero`               | `zero`               | `zero`               | PR 3         | momentum only, advection tests                                                                                                                                                                                                                                                                                              |
| `edmfx_*`, `pressure_work_tendency!`, `chemistry_tendency!` | —                                      | `n/a`                | `n/a`                | `n/a`                | `n/a`        | out of scope, see the contract                                                                                                                                                                                                                                                                                              |

### Implicit channel (`T_imp!`)

| Path                                          | Fields                       | ΔM                   | ΔW                   | ΔE                   | Collected by | Classification                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|:--------------------------------------------- |:---------------------------- |:-------------------- |:-------------------- |:-------------------- |:------------ |:------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `implicit_vertical_advection_tendency!`       | `ρ`, `ρe_tot`, tracers, `u₃` | `zero`               | `zero`               | `zero`               | PR 5         | Conservative transport, tested. The global zero covers this operator only. Precipitation leaves through `vertical_advection_of_water_tendency!`, whose lower boundary is open, and that is booked as its own boundary leg                                                                                                                                                                                                                                                            |
| `vertical_advection_of_water_tendency!`       | `ρ`, `ρq_tot`, `ρe_tot`      | `measured`           | `measured`           | `measured`           | PR 4         | **Sedimentation and fallout**, called from `implicit_tendency!` and therefore subject to implicit accepted-stage weighting and to the solve decomposition. `ᶜprecipdivᵥ` of the category flux with free outflow at the lower boundary, so this is where 1-moment water leaves the atmosphere. `Yₜ.c.ρ += vtt` beside `Yₜ.c.ρq_tot += vtt`, so mass and water take the same leg. A **boundary leg paired with the surface deposition leg**; the pair is measured, never assumed equal |
| `microphysics_tendency!`, 0-moment            | `ρq_tot`, `ρ`, `ρe_tot`      | `measured`           | `measured`           | `measured`           | PR 4         | The **default** path, since `implicit_microphysics` defaults on. Removal straight out of the column                                                                                                                                                                                                                                                                                                                                                                                  |
| `microphysics_tendency!`, 1-moment            | the categories               | `zero`               | `zero`               | `zero`               | PR 5         | Redistributes inside `ρq_tot`; the fallout leg is explicit, in `vertical_advection_of_water_tendency!`                                                                                                                                                                                                                                                                                                                                                                               |
| `surface_precipitation_tendency!`             | `Y.sfc.*`                    | `measured` (surface) | `measured` (surface) | `measured` (surface) | PR 4         | The implicit counterpart of the same surface leg                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `vertical_diffusion_boundary_layer_tendency!` | `ρe_tot`, tracers            | `measured`           | `measured`           | `measured`           | PR 5         | only when `diff_mode == Implicit()`                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `zero_velocity_tendency!`                     | momentum                     | `zero`               | `zero`               | `zero`               | PR 5         |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `edmfx_*`, `sgs_*`, `pressure_work_tendency!` | —                            | `n/a`                | `n/a`                | `n/a`                | `n/a`        | out of scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

Every implicit row is `unknown` for *stage weighting* until PR 5 establishes
that the accepted-solution coefficients can be read. The effect columns above
describe the tendency, not yet the accepted increment.

### Post-implicit channel (`T_post_imp!`)

| Path                                   | Fields             | ΔM        | ΔW        | ΔE        | Collected by | Classification                                                                                                                           |
|:-------------------------------------- |:------------------ |:--------- |:--------- |:--------- |:------------ |:---------------------------------------------------------------------------------------------------------------------------------------- |
| `correct_implicit_advection_tendency!` | `ρe_tot`, `ρq_tot` | `unknown` | `unknown` | `unknown` | PR 5         | Post-Newton upwind correction. Wired only when `energy_q_tot_upwinding != Val(:none)`. Its own leg, never folded into the implicit terms |

### A note on momentum-only tendencies

Every `zero` in the `ΔE` column for a drag or sponge row is exact by
construction, and the reason is worth stating once. `ρe_tot` is prognostic. A
tendency that changes only `uₕ` or `u₃` leaves `ρe_tot` untouched, so the total
energy contribution is exactly zero, and the diagnosed kinetic-energy change is
balanced by an equal and opposite diagnosed internal-energy change.

That is a statement about the implemented equations, not about the physics. If
a scheme is meant to deposit frictional heat, there is no implemented term
doing it. That belongs in the limitations register as a completeness gap, and
never in the numerical residual.

## Matrix 2 — state-mutation paths

The distinction that matters here is **authoritative** state against
**temporary** state. A change to a stage array, a work array, a cache, or a
diagnostic is not an endpoint source, even when it changes what a later
right-hand side computes. Booking it both as a correction and through the
resulting accepted right-hand side is double-counting.

| Path                                                                                               | Hook                | Authoritative? | ΔM         | ΔW         | ΔE         | Collected by | Classification                                                                                                                                                                                                                                                                                                                                                              |
|:-------------------------------------------------------------------------------------------------- |:------------------- |:-------------- |:---------- |:---------- |:---------- |:------------ |:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IMEX accepted update                                                                               | integrator          | yes            | `measured` | `measured` | `measured` | PR 5         | The reconciliation **envelope**, not an equation leg. It is the reference the per-path legs of Matrix 1 are reconciled against, and it may stand in for attribution that is not yet available. It is never summed alongside its own decomposition                                                                                                                           |
| `initialize_implicit_stage_problem!`                                                               | `initialize_imp!`   | no             | —          | —          | —          | —            | stage setup                                                                                                                                                                                                                                                                                                                                                                 |
| Newton stage solve                                                                                 | `T_imp!` + Jacobian | stage          | `measured` | `measured` | `measured` | PR 5         | `Q_solve_defect` carries the unconverged part                                                                                                                                                                                                                                                                                                                               |
| `limiters_func!` → SEM quasimonotone limiter                                                       | `lim!`              | yes            | `measured` | `measured` | `measured` | PR 6         | `Q_correction`                                                                                                                                                                                                                                                                                                                                                              |
| `limiters_func!` → `rescale_water_tags!`                                                           | `lim!`              | yes            | `zero`     | `zero`     | `zero`     | PR 6         | tag-only, sums to the parent by construction                                                                                                                                                                                                                                                                                                                                |
| `limiters_func!` → `enforce_mass_energy_consistency!`                                              | `lim!`              | yes            | `measured` | `zero`     | `measured` | PR 6         | `Q_correction`; moves `ρ` by `Δρq_tot` and `ρe_tot` by `Δρq_tot·(uᵥ(T)+Φ)`                                                                                                                                                                                                                                                                                                  |
| `limiters_func!` → vertical mass borrowing limiter                                                 | `lim!`              | yes            | `measured` | `measured` | `measured` | PR 6         | `Q_correction`                                                                                                                                                                                                                                                                                                                                                              |
| `dss!`                                                                                             | `dss!`              | yes            | `measured` | `measured` | `measured` | PR 6         | conservative in exact arithmetic on a closed sphere, so the measured amount is expected at reduction level and is a **finding if it is not**                                                                                                                                                                                                                                |
| `constrain_state!` → `prescribe_flow!`                                                             | `constrain_state!`  | yes            | `n/a`      | `n/a`      | `n/a`      | PR 6         | prescribed overwrite; out of scope, never a physical tendency                                                                                                                                                                                                                                                                                                               |
| `constrain_state!` → `tracer_nonnegativity_constraint!`, element variant, `constrain_qtot = false` | `constrain_state!`  | yes            | `zero`     | `zero`     | `zero`     | PR 6         | Category-only. The loop skips `ρq_tot`, so it clips `ρq_lcl`, `ρq_icl`, `ρq_rai`, `ρq_sno` and writes no parent variable                                                                                                                                                                                                                                                    |
| `constrain_state!` → `tracer_nonnegativity_constraint!`, element variant, `constrain_qtot = true`  | `constrain_state!`  | yes            | `measured` | `measured` | `measured` | PR 6         | `Q_correction`. It clips `ρq_tot` and hands the induced increment to `enforce_mass_energy_consistency!`, which moves `ρ` and `ρe_tot` with it                                                                                                                                                                                                                               |
| `constrain_state!` → `tracer_nonnegativity_constraint!`, vapour variant                            | `constrain_state!`  | yes            | `zero`     | `zero`     | `zero`     | PR 6         | Category-only at the default `constrain_qtot = false`. With `constrain_qtot = true` it clips `ρq_tot` **without** calling `enforce_mass_energy_consistency!`, so water moves while the stored energy does not: that combination records a measured water leg, an energy component of zero, and a physical-inconsistency note. It never gets an invented energy contribution |
| `constrain_state!` → `enforce_physical_constraints!`, non-EDMF branch                              | `constrain_state!`  | yes            | `zero`     | `zero`     | `zero`     | PR 6         | `enforce_grid_mean_microphysics_constraints!` clamps the four condensate fields and rescales them by `min(1, ρq_tot / ρq_cond)`. It reads `ρq_tot` and writes none of `ρ`, `ρq_tot`, `ρe_tot`, so it is invariant zero for all three parents. The EDMF branch is out of scope                                                                                               |
| `constrain_state!` → `repair_water_tag_partition!`                                                 | `constrain_state!`  | yes            | `zero`     | `zero`     | `zero`     | PR 6         | tag-only, sum preserved by construction                                                                                                                                                                                                                                                                                                                                     |
| `set_precomputed_quantities!`                                                                      | `cache!`            | no             | —          | —          | —          | —            | cache; never booked                                                                                                                                                                                                                                                                                                                                                         |
| `set_implicit_precomputed_quantities!`                                                             | `cache_imp!`        | no             | —          | —          | —          | —            | cache; never booked                                                                                                                                                                                                                                                                                                                                                         |
| `flux_accumulation!`                                                                               | discrete callback   | no             | —          | —          | —          | —            | mutates `Ref`s in `p` only                                                                                                                                                                                                                                                                                                                                                  |
| `external_driven_single_column!`                                                                   | discrete callback   | no             | —          | —          | —          | —            | refreshes forcing caches only                                                                                                                                                                                                                                                                                                                                               |
| `rrtmgp_solver_callback!`                                                                          | discrete callback   | no             | —          | —          | —          | —            | fills the radiation cache; the state effect arrives through `radiation_tendency!`                                                                                                                                                                                                                                                                                           |
| `nan_checking_callback`, `checkpoint_callback`, `gc_callback`, diagnostics                         | discrete callbacks  | no             | —          | —          | —          | —            | read-only with respect to `Y`                                                                                                                                                                                                                                                                                                                                               |
| `Setups.initial_state` / `overwrite_initial_state!` / `overwrite_from_file!`                       | initialization      | yes            | `n/a`      | `n/a`      | `n/a`      | PR 6         | sets `B⁰`; outside every transaction                                                                                                                                                                                                                                                                                                                                        |
| `handle_restart`                                                                                   | initialization      | yes            | `measured` | `measured` | `measured` | PR 6         | a **zero-duration transition** of its own, never charged to the next step                                                                                                                                                                                                                                                                                                   |

`update_constrain_state_every` decides how often the `constrain_state!` rows
fire. At the default `"step"` there is one firing per transaction. At `"stage"`
there is one per stage, and each is its own leg.

## What is not yet covered, and what it blocks

| Gap                                                      | Blocks                                                                     |
|:-------------------------------------------------------- |:-------------------------------------------------------------------------- |
| `Yₜ_lim` channel unread                                  | water closure, and energy closure wherever a limited tracer carries energy |
| Implicit accepted-stage weights unestablished            | ladder rung 2 for implicit terms                                           |
| `correct_implicit_advection_tendency!` unmeasured        | closure in every configuration with upwinding on, which is the default     |
| Coupled surface legs unmeasured                          | the coupled-view cancellation claim                                        |
| Energy leg of the `constrain_qtot = true` vapour variant | energy closure wherever that variant is configured                         |

Every one of these is a `unknown` that a later pull request turns into
`measured` or `zero`. None of them is allowed to become `zero` by assumption.
